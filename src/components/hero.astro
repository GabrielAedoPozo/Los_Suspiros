<main class="relative w-full h-screen m-0 p-0 overflow-hidden">

  <!-- Imagen de fondo -->
  <div class="absolute inset-0 w-full h-full">
    <img src="/Los_Suspiros/img/img2.jpg" alt="" class="w-full h-full object-cover">
  </div>

  <!-- Overlay -->
  <div class="absolute inset-0 bg-black/40 z-10"></div>
  
  <div class="relative z-20 flex items-center justify-center h-full px-8">
    <div class="text-center space-y-8">



      <h1 class="text-white text-5xl md:text-6xl lg:text-7xl font-light leading-tight drop-shadow-2xl">
        Simplemente Relájate.
      </h1>
            

      <!-- Indicador de scroll -->
      <div class="flex flex-col items-center gap-2 pt-8 animate-bounce">
        <span class="text-white/70 text-sm font-light">Descubre más</span>
        <svg class="w-6 h-6 text-white/70" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"/>
        </svg>
      </div>
    </div>
  </div>

</main>

